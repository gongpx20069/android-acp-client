name: Python PR prechecks

on:
  pull_request:
    paths:
      - "bridge/**"
      - ".github/workflows/python-pr-prechecks.yml"
  push:
    branches:
      - main
      - master
    paths:
      - "bridge/**"
      - ".github/workflows/python-pr-prechecks.yml"

jobs:
  bridge-python:
    name: Bridge Python tests and syntax
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install bridge package
        working-directory: bridge
        run: python -m pip install -r requirements.txt

      - name: Run unit tests
        run: python -m unittest discover -s bridge/tests -v

      - name: Check Python syntax
        run: python -m compileall -q bridge/run.py bridge/src bridge/tests

      - name: Run bridge launcher smoke test
        run: python bridge/run.py pairing --endpoint ws://127.0.0.1:4317 --no-qr
